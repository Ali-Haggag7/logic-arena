export const metadata = {
  title: "Bug Report | Logic Arena",
  description: "Found a bug in Logic Arena? Report it here and help us improve the platform.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

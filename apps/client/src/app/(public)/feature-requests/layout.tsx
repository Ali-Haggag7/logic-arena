export const metadata = {
  title: "Feature Requests | Logic Arena",
  description: "Suggest new features for Logic Arena. Vote on ideas and help shape the future of the platform.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

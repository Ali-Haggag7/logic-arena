export const metadata = {
  title: "Contact | Logic Arena",
  description: "Get in touch with the Logic Arena team. Report issues, ask questions, or share feedback.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export interface EvaluatorConstants {
  MAX_WHILE_ITERS: number;
  MAX_TICK_DURATION_MS: number;
}

export const CONSTANTS: EvaluatorConstants = {
  MAX_WHILE_ITERS: 10,
  MAX_TICK_DURATION_MS: 5,
};
